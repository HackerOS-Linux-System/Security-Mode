name: security
version: 0.1.0

authors:
  - HackerOS Team

description: |
  CLI tool for Security Mode, handling commands to interact with the core daemon via JSON files.

license: GPL-3.0

targets:
  security:
    main: main.cr

crystal: ">= 1.0.0"

dependencies:
  # No external dependencies needed, as it uses standard Crystal libraries
