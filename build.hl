// python3
// cargo
// crystal
// golang

>>> pip install gi
>>> pip install pyinstaller python-nmap scapy beautifulsoup4 requests paramiko pyyaml reportlab
>>> cd cli && crystal build src/main.cr --release
>>> cd env && cargo build --release
>>> cd pentest && cargo build --release
>>> cd gui && cargo build --release
>>> cd policy-engine && go get policy-engine && go build
